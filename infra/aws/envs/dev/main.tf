# Intentionally left simple.
# Terraform config is split into:
# - providers.tf : terraform + aws provider config
# - vpc.tf       : VPC module
#
# Later we can add EC2/bastion + outputs here or in separate files.
