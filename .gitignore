gh-actions-key.json

# ─────────────────────────
# Terraform local artefacts
# ─────────────────────────

# Local Terraform working directory (plugins, modules, etc.)
.terraform/

# State files (never commit these)
terraform.tfstate
terraform.tfstate.*

# Terraform crash logs (if Terraform blows up)
crash.log
crash.*.log

# Plan output files (if you ever run: terraform plan -out=plan.out)
*.tfplan
